import React from 'react';
import ReactDOM from 'react-dom';
import MapContainer from './containers/MapContainer/MapContainer.jsx';

ReactDOM.render(
    <MapContainer />,
    document.getElementById('root')
);